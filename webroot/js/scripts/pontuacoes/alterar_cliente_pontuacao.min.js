$(document).ready(function(){setInterval(function(){if($("#usuarios_id").val().length>0){$("#button_confirm").prop('disabled',!1);$("#button_confirm").prop('title','Clique para filtrar')}else{$("#button_confirm").prop('disabled',!0);$("#button_confirm").prop('title','Selecione um usuário para Alterar a pontuação atribuída')}},250)})